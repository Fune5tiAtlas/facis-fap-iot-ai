# QA Self-Assessment — Remediation Status

This document re-baselines the project's status against the external QA review's
findings, replacing blanket self-assessment with an accurate per-item status and
a pointer to the evidence for each. It records, for every review item, whether
the requirement is **Met**, **Partial** (implemented but evidence or a decision
is outstanding), **Deviation** (recorded in the deviation register), or **Open**.

Status is scoped to the deployed **ORCE-native** DSP connector (the Node-RED
flows under `services/dsp-connector/orce/`), which is the runtime the TDR
mandates. The connector has no Python implementation — the DSP connector is
ORCE-only.

## Verdict-decisive items (NF-1 – NF-4)

| Item | Requirement | Status | Evidence |
|---|---|---|---|
| **NF-1** | Identity & Trust (FR-IAM-001/002): DID resolution, VC/VP verification, reject invalid presentations with DSP error format | **Met** | `facis-dsp-iam-verify.json` (real did:web resolution, `jose.jwtVerify`, holder binding, jti replay cache, trusted-issuer allowlist, BitstringStatusList revocation); issuance in `facis-dsp-iam-issuance.json`; all four endpoint classes (catalogue, negotiation, transfer, ingest) link-call `iam.verify`; five QA test cases (valid/expired/missing/tampered VP, forged DID) in `tests/flows/iam-verify.spec.js` + `iam-verify-missing-auth.spec.js`; live unauthenticated `POST /dsp/transfers` returns 401 in enforce mode. **Config prerequisite**: enforce mode requires `DSP_TRUSTED_ISSUERS` populated (empty rejects all). |
| **NF-2** | Data Lake HTTP Ingest (FR-DL-001), QA flow #3: negotiate → transfer → ingest → Bronze row | **Met** | Provider serves real Trino rows (`facis-dsp-data.json`, no stub); consumer chain (`facis-dsp-consumer.json`) fetches via the access object, wraps a Bronze envelope, publishes to `dsp.ingest.raw` → NiFi PutSQL → `bronze.dsp_ingest`; consumer attaches a self-issued VP on its internal provider hops so flow #3 runs under enforce; E2E `tests/e2e/dsp-ingest-e2e.js` (live PASS, Bronze row count grew). `/dsp/ingest` is IAM-gated. |
| **NF-3** | Kafka Data-Plane Provisioning (FR-DP-002): real topic; no fabricated values | **Met** (topic) + **Deviation D-4** (credentials) | Real per-transfer topic create/delete on the broker via mTLS AdminClient (`dsp-tx-kafka-admin`); access objects credential-free by design (`sasl`/`token`/`url` null + `accessNote`); E2E `dsp-kafka-transfer-e2e.js` proves topic appears/disappears on the live broker. Credential delivery and ACLs are not buildable on this mTLS-only cluster — deviation D-4, pending client sign-off. |
| **NF-4** | Data-Sink deviation (Q-03): clarify and/or rework | **Met** + **Deviation D-1** | `docs/architecture/fap-role-mapping.md` (simulation proven a pure producer — zero consumer nodes); catalogue derived from live Trino gold tables (`dsp-cat-derive-fn`), the SRS MUST; lakehouse tooling relocated to `infrastructure/lakehouse/`; dedicated ORCE runtime for the connector; deviation D-1, pending RFC Q-03. |

## Follow-up items (NF-5 – NF-15)

| Item | Requirement | Status | Evidence / gap |
|---|---|---|---|
| **NF-5** | State persistence (SRS §6.1, PostgreSQL) | **Met** | Postgres StatefulSet + `DSP_PG_URI`; `facis-dsp-state.json` (DDL bootstrap, one-time legacy migration, transactional snapshot, boot retry, readiness guard); both stores global-scoped (fixed a bug that made transfer persistence a silent no-op); kill-pod durability drill passes live (`services/dsp-connector/orce/README.md` §Durability drill). Deviation D-2 covers the single-replica runtime. |
| **NF-6** | Header-trust / policy enforcement (FR-DL-010/011) | **Met** | AI-insight authorization derived from verified Keycloak tokens (`ai-insight-auth.json`), roles taken only from verified claims; header-injection negative test (`policy-rate-limit.spec.js`); DSP transfer path checks negotiation FINALIZED + counterparty DID (`dsp-tx-agreement-check`). |
| **NF-7** | DSP TCK & error payload (FR-DSP-001) | **Partial** | Typed DSP 2025-1 JSON-LD error bodies (`@context`/`@type`/pids/code/reason) + payload-shape tests (`dsp-error-binding.spec.js`) — **Met**. TCK harness + conformance gap register present (`services/dsp-connector/tck/`). Scope ruling recorded (`services/dsp-connector/tck/SCOPE-RULING.md`): the provider binding surface (canonical transfer paths + `dcat` catalogue JSON-LD) is implemented; asynchronous callbacks and consumer-role tests are demonstrator-scope deviations (D-5); the 201-vs-202 ACK conflict is a PMO ruling (NF-11). **TCK run executed (2026-07-23)** against an isolated IAM-off connector instance: **2 passed / 57 failed**. Honest finding — the raw run does **not** substantiate the scope ruling's "provider-synchronous subset passes" claim: the catalogue tests fail because the connector exposes real lakehouse datasets, not the TCK's fixture IDs (`Catalog01Test.assertDataset` expects `CAT0101`), and the `TP_C`/`CN` consumer-role + async tests are the D-5 deviations. The `tck-datasets.json` fixture overlay + procedure are delivered; injecting it live (catalogue then serves CAT0101-0103) makes `CAT:01-01` pass. `CAT:01-02/03` still fail on a TCK-side NPE (missing a dcat string field) and the `TP` subset needs FINALIZED-agreement seeding — so a fully green provider-synchronous subset remains a deep conformance-fixture engineering task (dcat-shape matching + agreement seeding), scoped but not converged. |
| **NF-8** | TLS 1.3 / S3 SSE / KMS / NetworkPolicies | **Met** (TLS, SSE) + **Deviation D-3** (KMS) + policies | TLS 1.3 minimum enforced at the ingress with a re-runnable evidence scan (`infrastructure/tls/`); S3 default encryption SSE-S3/AES256 enabled (`infrastructure/s3/`); external KMS proven infeasible on IONOS → deviation D-3; NetworkPolicies for the DSP datastores added to the chart (Postgres/Mongo reachable only from the DSP ORCE pod). Superset's separate Stackable ingress-nginx was brought to the same TLS 1.3 floor (L-4, 2026-07-23): TLS 1.2 refused on Superset + Keycloak + ai-insight-ui, verified live — the whole public surface is now TLS 1.3-min. |
| **NF-9** | Load tests (thresholds + p95) | **Partial — run executed, FAILS** | k6/Python scripts + p95 aggregator (`perf/`). **Trino latency run executed (2026-07-23, against the repaired healthy pipeline)**: #20 JDBC gold read **p95 6420 ms vs <1000 ms → FAIL**; FR-AI-001 AI retrieval **p95 6622 ms vs <500 ms → FAIL** (p50 already ~2.5 s). A real measured performance gap — the gold-read latency is ~6.5 s, consistent with the known Trino coordinator OOM / Iceberg small-file throughput problem (`project_trino_oom_followup`). Remediation is Trino memory tuning + MergeContent batching (a separate perf-engineering workstream), not a config fix. k6 DSP scripts + Kafka throughput remain to run (the DSP scripts need an IAM-off target or a VP). |
| **NF-10** | IONOS runtime evidence (rolling update, health) | **Partial** | Readiness probes present; the NF-5 kill-pod drill demonstrates in-flight state surviving pod loss. **Clean-install lifecycle exercised (2026-07-23, scratch `dsp-tck` namespace)** — surfaced and **fixed** three defects that broke a fresh `helm install`: (1) DSP state PVC requested 256Mi, below the IONOS CSI 1Gi floor (`f3b3c7d`); (2) the identity-key Secret was referenced but never rendered by the chart → `CreateContainerConfigError`, now optionally self-rendered from `dsp.iam.privateJwk` (`6e0a7f0`); (3) the post-install flow-deploy hook could time out on a slow cold start, now polls 7.5 min (`6e0a7f0`). **Full clean lifecycle validated end-to-end with the fixed chart (2026-07-24, scratch `dsp-tck`)**: install → `STATUS: deployed`, all pods Running, flows auto-deployed by the post-install hook; idempotent `helm upgrade` → rev 1→2 "Upgrade complete"; rolling/Recreate → pod recreated cleanly and Ready; `helm uninstall` → clean (0 resources). NF-10 lifecycle **closed**. |
| **NF-13** | Test hygiene / CI + BDD coverage | **Met** | CI runs the DSP, simulation, industrial-ingestion, SFTP, **and AI-insight-UI** ORCE flow specs; the `pytest` exit-5 ("no tests collected") tolerance is removed; `seedrandom` is declared in `orce/tests/package.json`; every Node suite ships a committed lockfile, and CI installs deps before running them (`npm ci` for the simulation and ai-insight-service suites; `npm install --include=dev` for dsp-connector and SFTP; the industrial-ingestion and ai-insight-UI suites declare zero runtime deps and need no install step); the local `npm test` invocation was corrected to `node --test flows/*.spec.js` so it runs on Node ≥ 22 as well (L-1); Docker Compose references removed. **BDD coverage**: SFTP gains a `pytest-bdd` suite (`tests/bdd`); dsp-connector and ai-insight-ui — deliberately Python-free (ORCE/JS-only) — cover the same scenarios as `node:test` flow specs + TCK/e2e/Playwright rather than a resurrected Python harness. The per-service behavioral coverage map and rationale are in `docs/test-strategy.md`. |
| **NF-14** | Self-assessment re-baseline | **Met** | This document. |
| **NF-15** | Configuration & deviations | **Met** | Deviation register consolidated (D-1..D-6) with a "Resolved — no deviation required" section for items fixed rather than deviated (TLS 1.3, in-memory→Postgres, dual-DSP→ORCE-only, Bronze hourly); gold-materializer single-sourced to `infrastructure/lakehouse/` (Helm copy deprecated, disabled by default). Bronze partitioning set to `hour(ingestion_timestamp)` in the table DDL. Default LLM model set to an IONOS AI Model Hub catalog model (`meta-llama/Llama-3.3-70B-Instruct`) across the acceptance config (service + UI `.env`, Helm values, ConfigMap); the exact id is confirmed at deploy time against `GET /v1/models`. |
| **NF-11** | Requirement-set defects | **Open** (PMO) | Not contractor code — a PMO ruling-request memo, tracked separately. |
| **NF-12** | Rate limiting | **Met** | Subject-keyed rate limiting in the AI-insight policy flow (NF-6), and a per-agreement sliding window now enforced on the DSP data-pull endpoint `GET /api/data/:assetId` (`dsp-data-verify-fn`, 429 + `Retry-After`, key = the HMAC-bound `agreementId`; `tests/flows/data-rate-limit.spec.js`). The "Redis token bucket" claim is reconciled honestly: it is an in-memory per-process window in Node-RED global context (resets on restart, would multiply across replicas), bounded by the single-replica ORCE runtime (deviation D-2); an externalized limiter is a scale-out follow-up. Documented in `services/dsp-connector/orce/README.md`. |

## Outstanding to reach Accept-with-Conditions

- **Runs** (need the QA cluster): NF-7 TCK to 100% (plus a scope ruling on the five registered gaps), NF-9 load-test p95 capture, NF-10 helm lifecycle capture.
- **Client/PMO decisions** (RFCs): D-1 (Q-03 Data-Sink), D-3 (object-store KMS), D-4 (Kafka credential model), and the NF-11 requirement-set ruling.
- **Cleanup follow-ups**: Superset subdomain TLS floor; broaden rate-limit enforcement (NF-12).
